#include <iostream> // Libreria
using namespace std; // Permite usar los elementos de std

struct temp { // Crea la estructura temp
    int i; 
    float f;
};

int main() {
    temp *ptr; // Declara un puntero a una estructura temp
    return 0;
}