#include <wx/wx.h>

// Definicion de la clase MainFrame que hereda de wxFrame
class MainFrame : public wxFrame {
public:
    // Constructor de la clase MainFrame
    MainFrame(const wxString& title);
};