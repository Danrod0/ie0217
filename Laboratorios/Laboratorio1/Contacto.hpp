#ifndef CONTACTO_HPP // Si no esta definido
#define CONTACTO_HPP // lo define

#include <iostream>
#include <string>

// Se define la estructura contacto, con los atributos de nombre y telefono tipo string
struct Contacto {
    std::string nombre;
    std::string telefono;
};

#endif // CONTACTO_HPP